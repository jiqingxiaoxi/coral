use Trinity to assembly simulated dataset:
  1, assembly the whole reads in every dataset
  2, assembly the reads align to coral or symbiodinium
  3, assembly the reads only align to coral, not the symbiodinium
